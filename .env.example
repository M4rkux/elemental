# Local database from docker-compose.yml; point at Neon in production.
# For db:migrate:prod / db:seed:prod, put the production URL in .env.production
# (gitignored), e.g. via: vercel env pull .env.production --environment=production
DATABASE_URL=postgres://elemental:elemental@localhost:5433/elemental
